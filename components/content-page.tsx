import { ArrowRight, CheckCircle2, LockKeyhole, ShieldCheck } from 'lucide-react'
import { Footer, Navbar, SectionHeading } from './site-shell'
import Link from 'next/link'

export function ContentPage({ eyebrow, title, intro, items, cta = 'Explore the platform' }: { eyebrow: string; title: string; intro: string; items: { title: string; body: string }[]; cta?: string }) { return <><Navbar /><main><section className="border-b border-border bg-card/20"><div className="container py-20 md:py-28"><div className="mono-label mb-5 text-primary">{eyebrow}</div><h1 className="max-w-4xl text-balance text-5xl font-semibold md:text-7xl">{title}</h1><p className="mt-7 max-w-2xl text-pretty text-lg leading-8 text-muted-foreground">{intro}</p></div></section><section className="section-pad"><div className="container grid gap-5 md:grid-cols-2">{items.map((item, i)=><article key={item.title} className="card-surface p-7"><div className="flex items-center justify-between"><span className="font-mono text-xs text-primary">0{i+1}</span><CheckCircle2 size={18} className="text-primary" /></div><h2 className="mt-8 text-xl font-semibold">{item.title}</h2><p className="mt-3 text-sm leading-7 text-muted-foreground">{item.body}</p></article>)}</div></section><section className="pb-24"><div className="container"><div className="card-surface flex flex-col gap-6 p-8 md:flex-row md:items-center md:justify-between"><div><div className="mono-label text-primary">FRONTEND FOUNDATION</div><p className="mt-3 max-w-xl text-sm leading-6 text-muted-foreground">The interface is structured for a future server-side processing and Supabase integration layer. No live detection or authentication is active in this phase.</p></div><Link href="/live-analysis" className="flex shrink-0 items-center gap-2 rounded-md bg-primary px-5 py-3 text-sm font-semibold text-primary-foreground">{cta}<ArrowRight size={16} /></Link></div></div></section></main><Footer /></> }

export const securityItems = [{title:'Voice privacy',body:'Treat raw voice as sensitive data. The future processing layer should minimize collection, exposure, and retention.'},{title:'Feature-based logging',body:'Prefer explainable features and results over retaining raw audio wherever the product workflow allows.'},{title:'Access control',body:'Role-aware experiences will be backed by server-side authorization and organization boundaries.'},{title:'Auditability',body:'Security-relevant events should be traceable through structured audit logs and configurable retention policies.'},{title:'Secure integration architecture',body:'Designed to sit between communication surfaces and protected actions without exposing service credentials.'},{title:'API security principles',body:'Future routes will validate inputs, scope data server-side, and keep secrets outside client bundles.'}]
export const featureItems = [{title:'AI voice detection',body:'Identify indicators associated with AI-generated, cloned, synthetic, and manipulated speech.'},{title:'Real-time analysis',body:'Analyze incoming audio in short segments so decisions can be supported during a conversation.'},{title:'Dynamic risk scoring',body:'Convert multiple signals into an understandable 0–100 impersonation risk score.'},{title:'Multi-layer analysis',body:'Combine acoustic, spectral, prosodic, speaker consistency, and contextual indicators.'},{title:'Explainable detection',body:'Present the primary contributors behind a future flag so the user knows what to verify.'},{title:'Alerts and verification',body:'Recommend trusted callback, MFA, supervisor approval, pause, or escalation at configurable thresholds.'},{title:'Privacy and analytics',body:'Support feature/result-based logging, auditability, and future organizational reporting.'},{title:'Integration APIs',body:'Prepare boundaries for banking systems, contact centers, enterprise communication, telecom, and fraud platforms.'}]
