'use client'
import Link from 'next/link'
import { useState } from 'react'
import { ArrowRight, ShieldCheck } from 'lucide-react'
const roles=['Frontline User / Employee','Security Analyst','Organization Admin','System Admin']
export function AuthForm({signup=false}:{signup?:boolean}){const [submitted,setSubmitted]=useState(false);return <main className="flex min-h-screen items-center justify-center bg-background px-4 py-12"><div className="w-full max-w-md"><Link href="/" className="mb-10 flex items-center justify-center gap-3"><span className="flex size-9 items-center justify-center rounded-lg border border-primary/50 bg-primary/10 text-primary"><ShieldCheck size={19}/></span><span className="font-mono text-sm font-bold tracking-[0.18em]">VOICEGUARD</span></Link><div className="card-surface p-7 md:p-9"><div className="mono-label text-primary">{signup?'CREATE ACCOUNT':'SECURE ACCESS'}</div><h1 className="mt-3 text-3xl font-semibold">{signup?'Join VoiceGuard':'Welcome back'}</h1><p className="mt-2 text-sm leading-6 text-muted-foreground">{signup?'Create your frontend profile. Authentication connects in Phase 2.':'Sign in interface foundation. No authentication is active in this phase.'}</p>{submitted&&<div className="mt-5 rounded-md border border-primary/40 bg-primary/10 p-3 text-xs text-primary">Frontend validation passed. Authentication is not connected yet.</div>}<form className="mt-7 flex flex-col gap-5" onSubmit={e=>{e.preventDefault();setSubmitted(true)}}>{signup&&<label className="flex flex-col gap-2 text-sm">Full name<input required className="rounded-md border border-input bg-background px-3 py-2.5 text-sm" /></label>}<label className="flex flex-col gap-2 text-sm">Email<input required type="email" className="rounded-md border border-input bg-background px-3 py-2.5 text-sm" /></label><label className="flex flex-col gap-2 text-sm">Password<input required minLength={8} type="password" className="rounded-md border border-input bg-background px-3 py-2.5 text-sm" /></label>{signup&&<><label className="flex flex-col gap-2 text-sm">Confirm password<input required minLength={8} type="password" className="rounded-md border border-input bg-background px-3 py-2.5 text-sm" /></label><label className="flex flex-col gap-2 text-sm">Role<select required className="rounded-md border border-input bg-background px-3 py-2.5 text-sm">{roles.map(x=><option key={x}>{x}</option>)}</select></label></>}{!signup&&<div className="flex justify-end"><Link href="/login" className="text-xs text-primary hover:underline">Forgot password?</Link></div>}<button type="submit" className="flex items-center justify-center gap-2 rounded-md bg-primary px-4 py-3 text-sm font-semibold text-primary-foreground">{signup?'Create frontend profile':'Login'} <ArrowRight size={15}/></button></form><p className="mt-7 text-center text-sm text-muted-foreground">{signup?'Already have an account? ':'Need an account? '}<Link href={signup?'/login':'/signup'} className="text-primary hover:underline">{signup?'Login':'Sign up'}</Link></p></div></div></main>}
