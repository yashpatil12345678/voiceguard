import { AppShell, EmptyState } from '@/components/app-shell'
const titles:Record<string,string>={sessions:'Call History',alerts:'Security Alerts',verification:'Verification Workflows',analytics:'Reports & Analytics',investigations:'Investigations',users:'Users',policies:'Risk Policies',reports:'Reports',integrations:'Integrations',admin:'System Administration',profile:'Profile'}
export default async function Page({params}:{params:Promise<{section:string}>}){const {section}=await params;const title=titles[section]??'Workspace';return <AppShell title={title}><div className="mb-8"><div className="mono-label text-primary">ROLE-BASED APPLICATION AREA</div><h2 className="mt-2 text-2xl font-semibold">{title}</h2><p className="mt-2 text-sm text-muted-foreground">Frontend foundation ready for future role-scoped data and workflows.</p></div><EmptyState title={`No ${title.toLowerCase()} yet`} description="Connect the backend services in a future phase to populate this workspace with secure, organization-scoped data."/></AppShell>}
