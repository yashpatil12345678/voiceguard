import Link from 'next/link'
export default async function Page({searchParams}:{searchParams:Promise<{message?:string}>}){const {message}=await searchParams;return <main className="flex min-h-screen items-center justify-center bg-background px-4"><div className="card-surface max-w-md p-8 text-center"><div className="mono-label text-danger">AUTHENTICATION ERROR</div><h1 className="mt-3 text-3xl font-semibold">This link is no longer valid</h1><p className="mt-3 text-sm leading-6 text-muted-foreground">{message === 'missing_code' ? 'The verification link was incomplete.' : 'The link may have expired or already been used. Request a new one and try again.'}</p><Link href="/login" className="mt-7 inline-flex rounded-md bg-primary px-4 py-3 text-sm font-semibold text-primary-foreground">Back to sign in</Link></div></main>}
